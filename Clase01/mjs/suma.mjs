// exportamos por default con este archivo .mjs
// Metodo moderno de export recomendado por Node y JavaScript

export function suma(a, b) {
  return a + b;
}

// Se puede exportar mas funciones

export function resta(a, b) {
  return a - b;
}
